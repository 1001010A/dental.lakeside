"use client";
import { Menu, X } from "lucide-react"; import { useEffect, useState } from "react"; import { buildInfoUrl } from "@/lib/whatsapp";
const links = [{ href: "#servicios", label: "Servicios" }, { href: "#nosotros", label: "Nosotros" }, { href: "#galeria", label: "Galería" }, { href: "#citas", label: "Citas" }, { href: "#preguntas", label: "Preguntas" }];
export function Header() { const [open, setOpen] = useState(false); useEffect(() => { document.body.classList.toggle("menu-open", open); return () => document.body.classList.remove("menu-open"); }, [open]); const close = () => setOpen(false); return <header className="sticky top-0 z-40 border-b border-primary bg-[var(--color-bg)]"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3 sm:px-6 lg:px-8"><a href="#inicio" className="serif text-primary text-2xl font-bold">Dental Lakeside</a><nav className="hidden items-center gap-6 lg:flex" aria-label="Navegación principal">{links.map((link) => <a className="text-sm font-bold hover:text-accent" key={link.href} href={link.href}>{link.label}</a>)}</nav><a className="button-primary hidden lg:inline-flex" href={buildInfoUrl()} target="_blank" rel="noopener noreferrer">Agendar cita</a><button className="text-primary lg:hidden" type="button" aria-label={open ? "Cerrar menú" : "Abrir menú"} aria-expanded={open} onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div>{open && <nav className="border-t border-primary bg-[var(--color-bg)] px-4 py-5 lg:hidden" aria-label="Menú móvil">{links.map((link) => <a className="block border-b border-primary py-4 font-bold" key={link.href} href={link.href} onClick={close}>{link.label}</a>)}<a className="button-primary mt-5 w-full" href={buildInfoUrl()} onClick={close} target="_blank" rel="noopener noreferrer">Agendar cita</a></nav>}</header>; }
